import { DocumentTypeToArrayPipe } from './document-type-to-array.pipe';

describe('DocumentTypeToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTypeToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
