import { TagThemeDirective } from './tag-theme.directive';

describe('TagThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new TagThemeDirective();
    expect(directive).toBeTruthy();
  });
});
